<div class="courses-course">
   <div class="course-info">
      <h1>{{course.title}}</h1>
      <img src="{{course.img}}" class="img-wrapper" alt="">
      <p>{{course.info}}</p>
      <p>{{course.price}}</p>
   </div>
</div>
